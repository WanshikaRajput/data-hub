

How I used AI

I used AI as a guide and helper, mainly for:

Concept explanation: understanding what a REST API, middleware, and CRUD logic actually are, and how Express handles routes.
Code ideas / starting structure: I asked for base code ideas for each phase (route scaffolding, in-memory CRUD, middleware, mock login) as a starting reference — not as final code.
Debugging and fixing terminal issues: several times my terminal got stuck, threw errors, or my server didn't restart properly. I described the exact error/screen and got help figuring out what was wrong and how to fix it (e.g. an unclosed quote in a git commit, running git add without a path, server not picking up code changes).
Tool setup guidance: step-by-step help downloading and setting up Postman, Git, GitHub, and Render, since I hadn't used these before.
Deployment troubleshooting: figuring out why the hardcoded port wouldn't work on Render, and fixing itCode ideas / starting structure: I asked for base code ideas for each phase (route scaffolding, in-memory CRUD, middleware, mock login) as a starting reference — not as final code.
Debugging and fixing terminal issues: several times my terminal got stuck, threw errors, or my server didn't restart properly. I described the exact error/screen and got help figuring out what was wrong and how to fix it (e.g. an unclosed quote in a git commit, running git add without a path, server not picking up code changes).
Tool setup guidance: step-by-step help downloading and setting up Postman, Git, GitHub, and Render, since I hadn't used these before.
Deployment troubleshooting: figuring out why the hardcoded port wouldn't work on Render, and fixing it.
The code in this repo is not a 1:1 copy of what the AI first suggested — I modified it, fixed issues I hit while running it locally, and added my own lines while getting it working. I used the AI mainly for direction, debugging help, and understanding concepts, not as a substitute for doing the work myself.
